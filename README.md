# packet-Sniffer
packet sniffer built in Python — captures and analyzes live network traffic including Ethernet frames, IPv4 packets, TCP/UDP segments, MAC addresses, ports, sequence numbers, and TCP flags. 
